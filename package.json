{
  "name": "oficina-mecanica-frontend",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Painel web da oficina. HTML/JS estatico, sem bundler, servido por S3 + CloudFront.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "serve": "npx --yes serve@14 -l 3000 ."
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.9"
  }
}
